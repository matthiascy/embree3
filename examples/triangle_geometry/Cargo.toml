[package]
name = "triangle_geometry"
version = "0.1.0"
authors = ["Will Usher <will@willusher.io>", "Yang Chen <matthiasychen@gmail.com>"]
edition = "2021"

[dependencies]
glam = "0.28"
embree3 = { path = "../.." }
support = { path = "../support" }

[profile.release]
debug = 1
